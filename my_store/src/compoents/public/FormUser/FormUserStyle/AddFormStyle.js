import styled from "styled-components";
import { Button, Modal, Checkbox, Form, Input, Spin } from "antd";

export const StyleAddUserModal = styled(Modal)`
  .ant-modal-footer {
    display: none;
  }
`;
